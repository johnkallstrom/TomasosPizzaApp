
@{
    ViewData["Title"] = "Inställningar";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<section id="settings">
    <div class="container">
        <h1>Inställningar</h1>
        <div>
            <input type="button" class="button" value="Profil" onclick="location.href='@Url.Action("EditDetails", "Account")'" />
        </div>
        <div>
            <input type="button" class="button" value="Konto" onclick="location.href='@Url.Action("EditAccount", "Account")'" />
        </div>
        <div>
            <input type="button" class="button" value="Ta bort" onclick="location.href='@Url.Action("Delete", "Account")'" />
        </div>
    </div>
</section>